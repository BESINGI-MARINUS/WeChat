doctype html
html(lang="en")
  head
    block head
      meta(charset="UTF-8")
      meta(name="viewport", content="width=device-width, initial-scale=1.0")
      title #{title}
      script(src="/socket.io/socket.io.js" defer)
      script(src='/js/bundle/index.js' defer)
  body
    block content
      